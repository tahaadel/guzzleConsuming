# guzzleConsuming
Rread an Api Endpoint using Guzzle(Http Client)

It's an example shows how to work with guzzle which really takes working with HTTP in PHP and puts it on Steroids. In addition to the simple requests, Guzzle can also manage json responses, fetch xml data, modify HTTP headers, upload data, send post requests.
